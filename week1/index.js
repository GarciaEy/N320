const mathFunctions = require("./functions");

// window refers to the browser window; node.js does not have a browser so it doesnt work
// console.log(window);
console.log("haiii");
console.log(mathFunctions.quadraticFormula);
